# Books Browser in JavaScript

This is an example to show how to use `esbuild-plugin-relay` in a minimal JavaScript application.

## Installation

```
cd examples/books-browser-javascript
npm install
npm run relay
npm run start:server
```

Open another terminal session and start the development server:

```
npm run start
```
